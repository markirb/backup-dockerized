# no implicit writes to the keystore dir, so we can mount it read only
#no locking
lock-never 
no-auto-check-trustdb
#avoid writes with cost of slower random generation
no-random-seed-file 

#fix duplicity error
use-agent
pinentry-mode loopback
